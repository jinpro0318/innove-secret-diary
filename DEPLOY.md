# AI 시크릿 일기장 배포 및 도메인 연결 가이드

이 프로젝트는 **Vercel**을 통해 배포하는 것이 가장 간편합니다. 아래 절차를 따라 `www.inaenote.com` 도메인을 연결해 보세요.

## 1. Vercel에 배포하기 (Deployment)

1.  **Vercel 로그인**: [https://vercel.com](https://vercel.com) 에 접속하여 GitHub 계정으로 로그인합니다.
2.  **새 프로젝트 추가**:
    *   대시보드에서 **[Add New...]** 버튼 -> **[Project]** 클릭.
    *   **[Import Git Repository]** 목록에서 `innove-secret-diary` 옆의 **[Import]** 버튼을 클릭합니다.
3.  **환경 변수 설정 (Environment Variables)**:
    *   `Configure Project` 화면에서 **Environment Variables** 섹션을 펼칩니다.
    *   아래 값들을 `.env.local` 파일 내용을 참고하여 입력합니다. (각각 입력 후 Add 클릭)
        *   `NEXT_PUBLIC_SUPABASE_URL`: (Supabase URL)
        *   `NEXT_PUBLIC_SUPABASE_ANON_KEY`: (Supabase Anon Key)
        *   `GEMINI_API_KEY`: (Gemini API Key)
4.  **배포 시작**: **[Deploy]** 버튼을 클릭하면 배포가 시작됩니다. (약 1~2분 소요)

## 2. 도메인 연결하기 (www.inaenote.com)

배포가 완료되면 Vercel이 제공하는 기본 주소(예: `innove-secret-diary.vercel.app`)가 생성됩니다. 이제 구매하신 도메인을 연결할 차례입니다.

1.  **Vercel 도메인 설정**:
    *   Vercel 대시보드에서 해당 프로젝트 클릭 -> 상단 **[Settings]** 탭 -> 좌측 **[Domains]** 메뉴 선택.
    *   입력창에 `www.inaenote.com`을 입력하고 **[Add]** 버튼을 클릭합니다.
    *   (`inaenote.com` 루트 도메인도 자동으로 추가할지 묻는다면 **[Add]**를 선택하여 둘 다 연결하는 것이 좋습니다.)

2.  **DNS 레코드 설정 (도메인 구입처)**:
    *   Vercel 화면에 **Invalid Configuration**이라고 뜨면서 필요한 DNS 레코드 값(A Record, CNAME 등)을 보여줍니다.
    *   도메인을 구매한 사이트(가비아, 후이즈, GoDaddy 등)의 **DNS 관리(DNS 설정)** 페이지로 이동합니다.
    *   Vercel이 알려준 값을 그대로 추가합니다.

    **예시 (일반적인 경우)**:
    *   **Type**: `CNAME`
    *   **Name (Host)**: `www`
    *   **Value (Target)**: `cname.vercel-dns.com`

    *   **Type**: `A`
    *   **Name (Host)**: `@` (또는 비워둠)
    *   **Value**: `76.76.21.21`

3.  **연결 확인**:
    *   DNS 설정 후 전파되는 데 최대 24시간이 걸릴 수 있으나, 보통 1시간 이내에 완료됩니다.
    *   Vercel 도메인 설정 화면에서 초록색 체크 표시(Valid)가 뜨면 연결 성공입니다!

## 3. Supabase URL 업데이트 (중요!)

배포 후 실제 도메인(`www.inaenote.com`)에서 로그인이 작동하려면, Supabase 설정도 업데이트해야 합니다.

1.  [Supabase Dashboard](https://supabase.com/dashboard) 접속 -> 프로젝트 선택.
2.  **Authentication** -> **URL Configuration** 메뉴로 이동.
3.  **Site URL**을 `https://www.inaenote.com`으로 변경합니다.
4.  **Redirect URLs**에 다음 주소들을 추가합니다:
    *   `https://www.inaenote.com/**`
    *   `https://www.inaenote.com/auth/callback`

이제 모든 설정이 완료되었습니다! 🎉
